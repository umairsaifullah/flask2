[phase.setup]
  aptPkgs = ['...', 'libreoffice']
  nixPkgs = ['...', 'libreoffice']
  commands = [
  "apt-get update",
  "apt-get install -y libreoffice"
]
  
[build]
builder = "NIXPACKS"
# Install dependencies
command = "apt-get update && apt-get install -y libreoffice"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
  internal_port = 6302
  protocol = "http"
  command = "apt-get update && apt-get install -y libreoffice"
